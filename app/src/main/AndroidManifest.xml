<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Necessário para listar os pacotes e suspendê-los no Android 11+ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Permissão para rodar serviço em background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColetorBloqueado">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SetupWizardActivity"
            android:exported="false"
            android:label="Configuração Inicial" />


        <activity
            android:name=".SettingsPasswordActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.ColetorBloqueado" />

        <!--
            Activity transparente de bloqueio.
            Intermediária usada pelo MonitoramentoService para forçar a troca de foreground
            de forma confiável em qualquer ROM, sem depender do BAL do serviço.
        -->
        <activity
            android:name=".BloqueioActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <receiver
            android:name=".MeuAdminReceiver"
            android:label="Administrador do Coletor"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Receptor para iniciar o serviço pós-boot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Receptor para permitir que outros apps solicitem a troca de Home -->
        <receiver
            android:name=".HomeChangerReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.brasil.coletorbloqueado.ACAO_SOLICITAR_ALTERAR_HOME" />
            </intent-filter>
        </receiver>

        <!-- Receptor para restaurar/alterar a Home padrão dinamicamente (ex: Mobile Butler) -->
        <receiver
            android:name=".HomeRestoreReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.brasil.coletorbloqueado.ACAO_RESTAURAR_HOME_PADRAO" />
                <action android:name="com.brasil.coletorbloqueado.ACAO_DEFINIR_HOME_PADRAO" />
            </intent-filter>
        </receiver>



        <!-- Serviço de Background -->
        <service android:name=".MonitoramentoService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitoramento de segurança corporativa e aplicação de políticas de dispositivo" />
        </service>

    </application>

</manifest>